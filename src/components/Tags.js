import React from 'react';

import styles from './tags.css';

const Tags = () => (
  <div className={styles.container} >
    <span className={styles.item}>Tags</span>
  </div>
);

export default Tags;
